import React from "react";
import * as s from "./styles";

const CardsSlider = ({ children }: { children: React.ReactNode }) => {
  return <s.Wrapper>{children}</s.Wrapper>;
};

export default CardsSlider;
