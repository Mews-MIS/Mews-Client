import styled from "@emotion/styled";

export const Wrapper = styled.div``;

export const CloseIcon = styled.div`
    display: flex;
    position: fixed;
    margin-top: 20px;
    margin-left: 20px;
    cursor: pointer;
`;